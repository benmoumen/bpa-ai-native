# BPA AI-Native Environment Configuration
# Copy this file to .env and update with your values
# Most defaults work for local development with Docker Compose

# =============================================================================
# Infrastructure Services (Docker Compose)
# =============================================================================

# PostgreSQL Database
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=bpa_ai_native
POSTGRES_PORT=5432

# Redis Cache
REDIS_PORT=6379
REDIS_PASSWORD=redis
REDIS_URL=redis://:redis@localhost:6379

# Keycloak Authentication
KEYCLOAK_PORT=8080
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# =============================================================================
# Application Configuration
# =============================================================================

# Node environment
NODE_ENV=development

# =============================================================================
# Web Application (apps/web)
# =============================================================================

# NextAuth.js secret - generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-key-generate-with-openssl-rand-base64-32

# NextAuth URL - the base URL of your application
NEXTAUTH_URL=http://localhost:3000

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:4000

# Keycloak OAuth2 Configuration
KEYCLOAK_URL=http://localhost:8080
KEYCLOAK_REALM=bpa
KEYCLOAK_CLIENT_ID=bpa-web
KEYCLOAK_CLIENT_SECRET=dev-secret-change-in-production

# =============================================================================
# API Application (apps/api)
# =============================================================================

# Server port
API_PORT=4000

# Database connection (uses POSTGRES_* values above)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/bpa_ai_native?schema=public

# Session timeout in minutes (NFR9: 30 minutes)
SESSION_TIMEOUT_MINUTES=30

# Keycloak connection timeout in milliseconds (NFR27: < 5 seconds)
KEYCLOAK_CONNECTION_TIMEOUT_MS=5000

# =============================================================================
# LLM Configuration (Phase 2)
# =============================================================================

# Groq API (primary LLM)
# GROQ_API_KEY=your-groq-api-key

# Anthropic API (fallback LLM)
# ANTHROPIC_API_KEY=your-anthropic-api-key

# LiteLLM Gateway (optional)
# LITELLM_API_URL=http://localhost:4001
